# Build outputs (the native module is cross-compiled by build.sh; the .hap by hvigor/DevEco).
entry/libs/
entry/.preview/
**/build/
.hvigor/
.cxx/
oh_modules/
local.properties
*.p7b

# Signing material, regenerated each build by setup-signing.mjs from the SDK certs + fixed cipher
# material (keystore/profile-cert copies, the signed provision profile, and material/).
signatures/
