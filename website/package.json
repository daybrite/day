{
  "name": "day-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "assemble": "node scripts/assemble-gallery.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
