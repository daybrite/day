<!-- Declared into the app manifest by `day build` via
     [package.metadata.day.android].manifest-components (docs/extending.md). Only the elements that
     belong inside <application>; the CLI adds the wrapper. Class names are fully qualified because
     this crate cannot know the app's package.

     Both receivers are exported="false": nothing outside this app has any business firing them. -->
<receiver
    android:name="dev.daybrite.day.notify.DayNotifyAlarmReceiver"
    android:exported="false" />
<receiver
    android:name="dev.daybrite.day.notify.DayNotifyBootReceiver"
    android:exported="false">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
        <action android:name="android.intent.action.QUICKBOOT_POWERON" />
    </intent-filter>
</receiver>
