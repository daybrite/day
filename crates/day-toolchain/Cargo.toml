[package]
name = "day-toolchain"
publish = true
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "Shared host-toolchain and SDK discovery for Day's build scripts and CLI."

[dependencies]
# Deliberately none: this crate is a build-dependency of piece/tweak crates and must stay tiny.
